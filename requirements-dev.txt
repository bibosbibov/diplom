# Зависимости только для разработки и тестов.
# Не подтягиваются в продакшен/Docker-образ.

# === Тесты ===
pytest>=7.4
pytest-cov>=4.1
pytest-mock>=3.12
pytest-httpx>=0.30

# === Линтеры и форматтеры ===
ruff>=0.4
black>=24.0
mypy>=1.8

# === Pre-commit hooks ===
pre-commit>=3.6

# === Jupyter ===
jupyter>=1.0
ipykernel>=6.27

# === Эталонная реализация CVSS (для сверки калькулятора в тестах, LGPLv3+) ===
cvss>=3.0
