# Зависимости только для разработки и тестов.
# Не подтягиваются в продакшен/Docker-образ.

pytest>=7.0
pytest-cov>=4.0
pytest-httpx>=0.30

# Референсная реализация CVSS от Red Hat — используется как эталон в тестах
# для сверки балла с нашим перенесённым калькулятором (LGPLv3+).
cvss>=3.0

black>=24.0
isort>=5.0
pylint>=3.0
mypy>=1.0
